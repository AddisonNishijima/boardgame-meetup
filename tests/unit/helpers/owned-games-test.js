
import { ownedGames } from 'boardgame-meetup/helpers/owned-games';
import { module, test } from 'qunit';

module('Unit | Helper | owned games');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ownedGames([42]);
  assert.ok(result);
});


<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <div class="outer-box">
      <div class="banner">
        <h2>Players</h2>
      </div>
      {{#each model as |player|}}
        {{player-tile player=player}}
      {{/each}}

    </div>
    <div>
      {{outlet}}
    </div>
  </div>
</div>

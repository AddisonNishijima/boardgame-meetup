<nav class='navbar'>
  <div class="row">
    <div class="col-md-10">
      <div class="row">
        <div class="col-md-7">
          {{#link-to 'index'}}<h1>Epic Board Game Night</h1>{{/link-to}}
        </div>
        <div class="col-md-5">
          <ul class='nav nav-justified'>
            {{#link-to 'index' tagName='li'}}
              <a {{bind-attr href="view.href"}}>Sessions</a>
            {{/link-to}}
            {{#link-to 'players'tagName='li'}}
              <a {{bind-attr href="view.href"}}>Players</a>
            {{/link-to}}
            {{#link-to 'games' tagName='li'}}
              <a {{bind-attr href="view.href"}}>Games</a>
            {{/link-to}}
          </ul>
        </div>
      </div>
    </div>
    <div class="col-md-2">
      {{login-display}}
    </div>
  </div>
</nav>

<div class="container">
  <div class="row">
    {{outlet}}
  </div>
</div>
